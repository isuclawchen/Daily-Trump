完成時間：2026-03-26
任務：檢查為何今日排程報告未發送至 Daily-Trump
結論：已定位原因並修正

直接原因：
1. `workspaces/issue-2/issue.md` 在 2026-03-26 00:00:36 UTC 的排程留言內容是「最新情況」，但該留言只有內容本身與尾端分隔線，沒有前置分隔線。
2. `.github/scripts/compose-coding-agent-prompt.mjs` 原本的 `extractContentAfterSeparator()` 會把尾端分隔線誤判成指令前分隔線，導致這則排程留言被解析成空字串。
3. `issue-2-post-publish` workflow 因此在 2026-03-26 00:04:42 UTC 把 alias mode 判成 `normal`，沒有進入 `Publish styled Markdown to Daily-Trump` 步驟，所以 `workspaces/issue-2/artifacts/scheduled-20260326/report.md` 沒有被送到 `isuclawchen/Daily-Trump`。

已處理：
1. 已修正 `.github/scripts/compose-coding-agent-prompt.mjs`，讓只有「指令內容 + 尾端分隔線」的排程留言也能正確解析。

驗證結果：
1. 對目前 `workspaces/issue-2/issue.md` 重跑 `--detect-alias`，結果已由 `normal` 變成 `alias`。
2. 以臨時檔重現「排程留言只有最新情況 + 尾端分隔線」的邊界案例，結果為 `alias`。
3. 以一般非 alias 留言測試，結果維持 `normal`。

補充：
1. 今天 2026-03-26 已經跳過的那次後置發佈不會自動回補，需要手動重跑 `Issue-2 後置發佈 Daily-Trump` workflow，或再觸發一次符合條件的發佈流程。